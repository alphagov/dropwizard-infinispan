server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0
  requestLog:
    appenders:
      - type: console

logging:
  level: DEBUG
  appenders:
    - type: console

infinispan:
  bindAddress:
  initialHosts:
  clusterName:
  type: standalone
  expiration: 8h
  persistenceToFileEnabled: false
